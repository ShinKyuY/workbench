{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "printf 'NOTE: The rules below apply to every Korean sentence you write.\\n\\n'; awk '/^## /{if(f) exit} /^## Language & Writing/{f=1} f; END{if(!f) print \"WARN: Language & Writing section not found\"}' \"${CLAUDE_PLUGIN_ROOT}/CLAUDE.md\"",
            "statusMessage": "Writing check"
          }
        ]
      }
    ]
  }
}
